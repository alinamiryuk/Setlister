const mongoose = require('mongoose')

module.exports = mongoose.model('festival', {
  userName: {
    type: String,
    required: true,
    unique: true,
  },
  email: {
    type: String,
    required: true,
    unique: true,
  },
  password: {
    type: String,
    required: true,
  },
 })
